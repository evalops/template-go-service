version: "2"

run:
  timeout: 5m
  relative-path-mode: wd

linters:
  enable:
    - errcheck
    - gosec
    - govet
    - staticcheck
    - unused

  settings:
    errcheck:
      check-type-assertions: true

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
